/* Автогенерация: npm run env:generate (читает TWOGIS_API_KEY и frontend/.env) */
/* Не коммитьте этот файл с реальным ключом — держите секрет в .env (не в git). */
export const TWOGIS_API_KEY_FROM_DOTENV = '';
